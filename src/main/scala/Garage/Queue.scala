package Garage

import scala.collection.immutable.ListMap

class Queue extends Garage {

  customerMap

  def arrangeCustomers = {
    var totTime = 0
    for((k,v) <- customerMap) {
      if(v.totalTimeToFix > totTime){

      }
  }
    for ((cust,veh) <- customerMap) {
    }
  }

}
